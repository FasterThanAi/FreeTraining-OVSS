% =====================================================================
%  It's the Labels, Not the Model
%  ROADMAP Phase 5. Section map follows PAPER_OUTLINE.md §3.
%
%  Compiles as-is with pdflatex + a standard TeX Live. To move to the
%  CVPR workshop template, replace the two lines marked TEMPLATE with
%  \documentclass[10pt,twocolumn,letterpaper]{article} + \usepackage{cvpr}
%  (or IEEEtran for GRSL). Nothing else here assumes a class.
%
%  ⚠️ EVERY NUMBER COMES FROM numbers.tex. Do not type a figure inline --
%  if it is not a macro there, it has no citation and does not belong.
% =====================================================================
\documentclass[10pt,a4paper]{article}                        % TEMPLATE
\usepackage[margin=2.2cm]{geometry}                          % TEMPLATE

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath,amssymb}
\usepackage{multirow}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}

\graphicspath{{../docs/}}
\input{numbers}

\newcommand{\todo}[1]{\textcolor{red}{\textbf{[TODO: #1]}}}
\newcommand{\best}[1]{\textbf{#1}}
\newcommand{\bg}{\texttt{background}}

\title{It's the Labels, Not the Model:\\
       Why Presence-Gated Segmentation Discards, and Why Recovery Doesn't Help}
\author{\todo{authors, affiliation}}
\date{}

\begin{document}
\maketitle

\begin{abstract}
Training-free open-vocabulary segmentation pipelines built on SAM~3 gate their
predictions by a confidence threshold and assign everything below it to a
\bg{} class. We show this discards a great deal of real land cover: \discardPct{} of all
real-class pixels on LoveDA, \discardPx{} in total, with silence outnumbering
misclassification \discardRatio{}. The size of that residual, and whether it can
be detected at all, is governed by the dataset's \emph{label design} rather than
by the model. Where \bg{} is a
catch-all covering \bgShareLove{} of the scene, the residual is large and no
signal separates it from genuine background (nine tested; best AUC \aucLove{}
against a \aucFloor{} floor); where the vocabulary covers the scene
(\bgShareOEM{}), only \discardOEM{} is discarded and the runner-up class score
separates it at AUC \aucOEM{}. Recovery is nonetheless not worthwhile in either
regime: it yields \recoverLove{}~mIoU on LoveDA, and on OpenEarthMap its
apparent \recoverOEM{} is entirely attributable to \bg{} ceasing to be
over-predicted (\oemBgGain{}) while the real classes lose \oemRealLoss{}~IoU.
We establish this causally rather than by correlation: because the catch-all's
share is set by the vocabulary, we manipulate it directly, and against an
arity-matched control detectability falls \ivAtwo{}~$\to$~\ivBtwo{} while the
control moves to \ivDtwo{}. A second intervention shows the lever is the label
space and not the prompt list.
A different correction does work. We fit one threshold \emph{per class} rather
than one globally, on \calibTiles{} labelled tiles, with no model weights trained;
this is the protocol the baseline already uses to tune its single threshold. It
gives \methodGain{}~$\pm$~\methodSD{}~mIoU on LoveDA, capturing
\oracleShare{} of an oracle bound of \oracleBound{}, and it is positive on both
datasets once the catch-all is excluded from the metric (\mrLoveReal{} and
\mrOemReal{}~mIoU). \best{The correction is not specific to SAM~3, but it is specific to the
dataset.} Applied unchanged to the nearest published competitor --- a CLIP-based
pipeline with its own tuned threshold --- it adds \cnCV{}~$\pm$~\cnCVsd{}~mIoU on
LoveDA with every fold positive, so our method composes with that work rather than
competing with it. On OpenEarthMap it helps neither pipeline's headline, and for
the same reason in both: the fit must sacrifice a pathologically calibrated
catch-all that owns a ninth of the metric. That exclusion is itself a recommendation: mIoU is an
unweighted mean, so a catch-all owns $1/N$ of the score however meaningful it is,
and we observe it moving the headline in both directions.
Finally, we show the oracle bound is not reachable without labels, and identify
why: \nAttempts{} label-free rules fail, four of which provably estimate per-class
precision, because the optimal threshold solves a \emph{coupled} multi-class
objective that no per-class statistic can express.
\end{abstract}

% =====================================================================
\section{Introduction}
\label{sec:intro}

Open-vocabulary semantic segmentation promises to label an image against a list
of class names in plain English, with no training on the target dataset. For
remote sensing this is unusually valuable: annotation is expensive, and it must
be redone for every new region and sensor. Recent work replaces the multi-model
CLIP stacks that dominated this area with SAM~3, whose presence, semantic and
instance heads can be combined into a single pipeline that reports strong
results across twenty datasets~\cite{segearthov3}.

That pipeline ends in a threshold. Class scores are gated by a presence score,
the highest-scoring class is taken per pixel, and any pixel whose winning score
falls below a threshold $\tau$ is assigned to \bg{}. The threshold is tuned per
dataset using labels, and everything below it is given up on.

We measure what that costs. On LoveDA, \best{\discardPct{} of all real
land-cover pixels --- \discardPx{} of them --- are assigned to \bg{}}, and
across every class discard outnumbers misclassification by roughly
\discardRatio{}. The dominant failure of this pipeline is not that it is wrong;
it is that it is \emph{silent}.

The natural response is to recover those pixels; the natural objection is that a
lower threshold would do it more cheaply. Neither works
(Table~\ref{tab:interventions}). Lowering $\tau$ from $0.5$ to $0.1$ recovers
two-thirds of the residual and costs \tauCost{}~mIoU, at \tauRatio{} wrong pixels
per right one; removing the presence gate costs \presCost{}. Our original
proposal, a region-level recovery method built on a corpus-level co-occurrence
prior, adds \priorGain{} over copying the largest neighbouring region, where a
\emph{perfect} matrix adds \priorOracle{}.

Running the same pipeline on a second dataset explains why, and the explanation
is the paper's main contribution. On OpenEarthMap, whose \bg{} class is a rare
genuine label rather than a catch-all, only \discardOEM{} is discarded, there
are no catastrophic tiles, and the residual is easy to detect --- the runner-up
class score reaches AUC \aucOEM{} where on LoveDA nine separate signals sit at
chance (Fig.~\ref{fig:auc}). \best{Detectability is a property of the annotation scheme, not of the
model.} A catch-all class gives the model a plausible answer everywhere, so a
strong second-place score carries no information about whether a real class was
suppressed. We do not leave this as a correlation across two datasets. The
catch-all's share is set by the vocabulary, so we \emph{intervene} on it
(Section~\ref{sec:intervention}), against a control that makes the same
vocabulary change without raising the share; the effect follows the share and not
the class count.

That points somewhere more productive than recovery. One global threshold is
serving classes with very different precision--recall profiles: on LoveDA
\texttt{water} is right 89.5\% of the time it fires but finds only
54.7\% of what is present, while \texttt{agricultural} is balanced.
Fitting one threshold per class on \calibTiles{} labelled tiles, with no weights
trained, gives \methodGain{}~$\pm$~\methodSD{}~mIoU with the catch-all untouched
at
\methodBg{}~IoU, so the gain is land cover and not a repaired catch-all.

\paragraph{Contributions.}
\begin{enumerate}\itemsep2pt
  \item \textbf{A mechanism, established causally.} The catch-all's share of
        ground truth (\bgShareLove{} vs \bgShareOEM{}) tracks the residual's size,
        its detectability, the presence--discard correlation and the
        catastrophic-tile count across two datasets
        (Table~\ref{tab:mechanism}). We then manipulate that share through the
        vocabulary, with class count controlled and predictions registered before
        each run, and show the effect follows the share
        (Table~\ref{tab:intervention}). A further intervention localises it to the
        label space rather than the prompt list.
  \item \textbf{A method.} Calibrated per-class thresholds:
        \methodGain{}~$\pm$~\methodSD{}~mIoU on LoveDA, catch-all \methodBg{},
        \oracleShare{} of an oracle bound of \oracleBound{}, verified end-to-end in
        the pipeline, and positive on \emph{both} datasets on the
        catch-all-excluded metric (\mrLoveReal{}, \mrOemReal{}). \best{It is not
        backbone-specific but is dataset-specific}: on a CLIP-based competitor at
        its own operating point it gives \cnCV{}~$\pm$~\cnCVsd{} on LoveDA and
        nothing on OpenEarthMap, exactly as for our own pipeline
        (Table~\ref{tab:transfer}).
  \item \textbf{A reporting recommendation.} We observe the catch-all moving the
        headline mIoU in both directions, supplying \oemBgGain{}~IoU of an
        apparent gain in one experiment and cancelling a real one in another, and
        recommend reporting mIoU over the real classes beside it.
  \item \textbf{A bound, with its reason.} \nAttempts{} label-free rules fail to
        reach that bound. Four provably estimate per-class precision, which we had
        supposed was the missing quantity; it is not.
  \item \textbf{A null-model correction} for co-occurrence analysis on segmentation
        masks, which refuted one of our own earlier findings and generalises beyond
        this setting.
\end{enumerate}

Every negative result is reported with an oracle bound, meaning what a perfect
version of the missing component would have bought, so a reader learns where the
headroom is rather than only that we failed to reach it.

% =====================================================================
\section{Related work}
\label{sec:related}

\paragraph{Training-free open-vocabulary segmentation for remote sensing.}
SegEarth-OV~\cite{segearthov} established the setting, introducing a feature
upsampler trained on unlabelled imagery to recover the spatial detail that
CLIP's $1/16$ downsampling destroys. Its successor,
SegEarth-OV3~\cite{segearthov3}, replaces the CLIP stack with SAM~3~\cite{sam3}
and contributes two components we inherit: \emph{dual-head mask fusion}, which
takes the elementwise maximum of the dense semantic head and the aggregated
instance head, and \emph{presence-guided filtering}, which multiplies each class
map by a scalar presence score before thresholding. It is our baseline
throughout, and we reproduce it to within rounding on both datasets.

\paragraph{Dense prediction from vision--language models.}
The training-free line begins with the observation that CLIP~\cite{clip} carries
usable dense information once the pooling that destroys it is
removed~\cite{maskclip}, and continues through corrections to its
attention~\cite{sclip,clearclip} and to the features guiding it~\cite{proxyclip};
grouping-based alternatives learn segment structure from text supervision
alone~\cite{groupvit}. SAM~3 changes the setting rather than extending it:
presence, semantic and instance heads arrive in one model, so the pipeline
collapses to prompting and thresholding. Its instance head inherits
DETR's~\cite{detr} set prediction and MaskFormer's~\cite{maskformer} mask
classification, its semantic head is FCN-style~\cite{fcn}, and the promptable
setting is SAM's~\cite{sam}. Our results concern what that final threshold does,
so they apply to any pipeline in the family that ends in one.

\paragraph{Thresholds and calibration.}
The step we study is a decision rule over per-class scores, and that such scores
are systematically miscalibrated is well established~\cite{calibration}. What is
specific here is that the pipeline is training-free, so the usual remedies are
available only in the restricted sense the baseline's own threshold already is: a
small labelled calibration set and nothing more. We show what that budget buys
and bound what can be done without it. Classical automatic
thresholding~\cite{otsu} is among the label-free rules we test, and it does not
reach the bound.

\paragraph{Context at inference.} ConInfer~\cite{coninfer} is the nearest
competitor: it fits a Gaussian mixture over self-supervised patch
features~\cite{dinov2} and fuses the posterior with the vision--language prior,
reporting $+2.80$~mIoU. Its context is \emph{visual}, at \emph{patch} level,
fitted \emph{per scene}; our original proposal was semantic context at region
level mined corpus-wide, which ConInfer's own limitations name as future work. We
report it as an ablation rather than a contribution because we measured it and it
does not earn its place (Section~\ref{sec:built}). ConInfer's gain should
\emph{not} be read as evidence that appearance solves the detection problem we
identify: it performs context modelling, not residual-class detection.
\best{The two are orthogonal, and we demonstrate it}: ConInfer changes the
per-class \emph{scores}, our method changes the \emph{decision rule} applied to
them, and applying ours on top of theirs improves their pipeline
(Section~\ref{sec:transfer}).

\paragraph{Spatial context as post-processing.} Dense CRFs~\cite{densecrf}, their
differentiable successors~\cite{crfasrnn} and global-context encodings of the
label distribution~\cite{encnet} are the classical answer to ``use spatial
context to fix segmentation'', and remain the baseline a reviewer will raise
against any neighbour-propagation scheme. Our findings are relevant to that whole
family: we show that on a catch-all dataset the limiting factor is not the
propagation rule but the inability to identify which pixels should be propagated
to at all. The atoms such a scheme propagates over matter more than the rule ---
we compare connected components against superpixels~\cite{slic,felzenszwalb} and
find a twenty-point difference in the ceiling (Section~\ref{sec:built}).

\paragraph{Benchmarks.} Both datasets are standard
here~\cite{loveda,openearthmap}, and a recent benchmark reformulates several
under one open-vocabulary protocol~\cite{ovrsisbench}. Our reporting
recommendation (Section~\ref{sec:results}) is aimed at such efforts: any
benchmark supplying a catch-all hands one class $1/N$ of the headline metric, and
we show this is not hypothetical.

% =====================================================================
\section{Setup}
\label{sec:setup}

We evaluate on LoveDA~\cite{loveda} and OpenEarthMap~\cite{openearthmap}, which
differ in exactly the property this paper turns out to depend on: LoveDA's
\bg{} is a catch-all covering a third of every scene, while OpenEarthMap's
vocabulary very nearly covers it.

\paragraph{Datasets.} LoveDA~\cite{loveda} validation, \loveTiles{} tiles at
$1024^2$ and 0.3\,m GSD, seven classes including \bg{}, evaluated at
the baseline's published $\tau=0.5$. OpenEarthMap~\cite{openearthmap} validation,
\oemTiles{} of the official 500 tiles, nine classes, at its published $\tau=0.1$.
We reproduce the baseline at \loveBase{}~mIoU on LoveDA against a published
\lovePub{}, and \oemBase{} on OpenEarthMap against a published \oemPub{}. At 77\% of the OpenEarthMap split, and with a
different tile mix, the latter is a sanity anchor rather than a reproduction
gate. A broken class-name to label-value mapping would still have landed far off
in one direction, so it establishes that the preparation is sound.

\paragraph{Instrumentation and its validation gate.} All measurements come from
an instrumented copy of the baseline segmentor that records, per pixel, the
winning class and score, the runner-up, the pre-gating fused score, and each
head's own top-1, plus the per-class presence score per sliding-window view.
\best{Every instrumented run must reproduce \loveInstr{}~mIoU and \discardPct{}
discard exactly}; a run that does not has changed behaviour and is rejected. This
gate is not a formality --- it is what establishes that the additions are
observation-only, and every number below inherits it.

\paragraph{A cached sufficient statistic.} Because the argmax does not depend on
$\tau$, the histogram $H[\text{gt},\text{pred},\text{conf bin}]$ is a sufficient
statistic for the confusion matrix at \emph{any} per-class threshold vector.
Caching it once turns every subsequent threshold sweep, oracle bound and
cross-validation into a CPU operation on a saved array rather than a GPU run. We
verify it against the deployed pipeline in Section~\ref{sec:results}.

% =====================================================================
\section{Anatomy of the residual}
\label{sec:anatomy}

\paragraph{Two mechanisms, not one.} A real-class pixel can be labelled \bg{} in
two ways: its winning score falls below $\tau$, or \bg{} wins the argmax outright
at $\mathrm{conf}\ge\tau$. On LoveDA, \mechA{} of the residual is the first and
\mechB{} the second --- \mechBpx{} pixels in \mechBtiles{} tiles, \emph{all of
them water}, in large homogeneous scenes where the presence head fires for \bg{}.
The distinction matters because \best{the second group is unreachable by any
threshold}: lowering $\tau$ cannot change an argmax. Part of the residual lies
outside what threshold tuning can address at all.

\paragraph{Threshold relaxation is non-linear, and expensive where it matters.}
Lowering $\tau$ from $0.5$ to $0.3$ recovers 99M pixels for
$0.73$~mIoU; from $0.3$ to $0.1$ recovers a nearly identical count for $4.81$.
The residual is not a uniform mass: a shallow band sits just below the operating
point and a deep tail does not. Tracing the confusion matrix, the full sweep
recovers 85.3M correct agricultural pixels while creating
147.9M false ones --- \tauRatio{} wrong per right. A scalar
threshold carries no information about \emph{which} class a region plausibly is,
so it cannot recover selectively.

\paragraph{Presence gating is a correlate, not a cause.} Tiles that fail
catastrophically ($\ge$99\% discarded, $n=\catTilesLove{}$) have a
median maximum presence score of $0.273$ against $0.918$ on healthy tiles, and
the correlation over all \loveTiles{} tiles is \presCorrLove{}. On one such tile
the semantic head emits a $+10.13$ logit for \texttt{road} --- effective
certainty --- while the presence gate crushes the tile's best achievable score to
$0.076$. We proposed that gating was destroying recoverable evidence, and then
refuted it: with gating removed, recovery on those tiles is \emph{uncorrelated}
with how suppressed they were ($r=+0.018$), the same intervention pushes healthy,
barely-gated tiles from 0.46\% to 54.11\% discard, and
mIoU falls by \presCost{}. The correlation is partly mechanical --- since
$P_{\text{final}} = P_{\text{fused}}\cdot S_{\text{pres}}$, a low presence score
\emph{forces} discard whether or not gating is at fault --- and we report it as an
observation only.

That counterfactual produced an unexpected finding. \best{SAM~3 essentially never
detects \bg{}}: its median presence score is $0.022$, against $0.45$--$0.91$ for
every real class, because \bg{} is not a visual concept but the dataset's
catch-all. The gate's largest measured effect is therefore \emph{suppressing
\bg{}} so that $\tau$, rather than the argmax, governs background assignment ---
which inverts the motivation the baseline paper gives for it.

% =====================================================================
\section{The mechanism: two datasets}
\label{sec:mechanism}

\begin{table}[t]\centering\small
\caption{Every measured quantity moves with the catch-all class's share of ground
truth. Same pipeline, same code, two datasets.}
\label{tab:mechanism}
\begin{tabular}{lrr}
\toprule
 & LoveDA & OpenEarthMap \\
\midrule
baseline mIoU (published)         & \loveBase{} (\lovePub{}) & \oemBase{} (\oemPub{}) \\
\best{\bg{} share of ground truth} & \best{\bgShareLove{}} & \best{\bgShareOEM{}} \\
real-class pixels discarded       & \discardPct{}   & \discardOEM{} \\
catastrophic tiles ($\ge$99\%)    & \catTilesLove{} & \catTilesOEM{} \\
corr(presence, discard)           & \presCorrLove{} & \presCorrOEM{} \\
best detection AUC                & \aucLove{}      & \aucOEM{} \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure*}[t]\centering
\includegraphics[width=\textwidth]{fig2_mechanism.pdf}
\caption{The catch-all class's share of ground truth as a common cause. Six
independently measured quantities --- residual size, detection AUC, the
presence--discard correlation, the catastrophic-tile count, the fraction of
pixels the model commits to, and the achievable recovery --- separate the two
datasets in the same direction.}
\label{fig:mechanism}
\end{figure*}

Table~\ref{tab:mechanism} and Fig.~\ref{fig:mechanism} are the paper's central
observation. Six quantities
measured independently, by the same code on the same pipeline, separate the two
datasets in the same direction, and they are ordered by a single property of the
\emph{annotation scheme}: whether the vocabulary covers the scene.

The explanation is direct. LoveDA's \bg{} absorbs a third of every image, so the
model always has a plausible answer available, and a strong runner-up score means
nothing --- it could indicate a suppressed real class, or a background region that
merely resembles one. Its runner-up AUC is $0.541$. OpenEarthMap's \bg{} is a rare
marker for genuinely unlabelable pixels, so a strong runner-up does indicate a
suppressed real class, and the same signal reaches \aucOEM{}.

\paragraph{Two of our own claims do not generalise, and we label them.} The
presence-collapse correlation of Section~\ref{sec:anatomy} (\presCorrLove{} on
LoveDA, \catTilesLove{} catastrophic tiles) becomes \presCorrOEM{} with
\catTilesOEM{} catastrophic tiles on OpenEarthMap. It is a property of LoveDA's
annotation scheme, not of SAM~3. Likewise \best{\discardPct{} is not a general
figure} --- the identical pipeline discards \discardOEM{} on OpenEarthMap. Both
are stated here rather than left for a reader to discover.

% =====================================================================
\paragraph{Confusability is not the driver.} Across two datasets the catch-all's
share moves together with its \emph{confusability} --- how much of it the model
gives a real class --- and either could explain the ordering. LoveDA's urban and
rural domains separate them, and in opposite directions: rural has the larger
share (\SHAREr{} of ground truth against \SHAREu{}) while urban's catch-all is
much the more confusable (\CONFu{} of it escapes into real classes, against
\CONFr{}). The two hypotheses therefore name \emph{different} strata as the one
that should detect worse, and share is right: AUC is \AUCr{} in rural against
\AUCu{} in urban, so the more confusable stratum is the one where detection works
\emph{better}. We note what this does and does not settle. It eliminates
confusability as the driver. It does not explain the gap between those two strata,
which is large and, by the saturation result below, cannot be attributed to their
difference in share either; we leave that unexplained rather than fitting a story
to it.

\subsection{Catch-all share, intervened on}
\label{sec:intervention}

The evidence so far is observational: across datasets and across LoveDA's urban
and rural strata, detectability tracks the catch-all's share. Stratification
cannot rule out everything those strata differ in. But catch-all share is not a
property one has to go looking for --- \best{it is set by the vocabulary}. So we
manipulate it.

Every class in the baseline is an independent forward pass with its own text
prompt, and the only cross-class operation in the pipeline is the \texttt{argmax}.
Dropping a class from the vocabulary is therefore \emph{exactly} equivalent to
dropping its channel, and one cached score stack answers every vocabulary
question. All \ivArms{} arms read identical model outputs, so they differ in the
vocabulary and in nothing else --- there is no run-to-run variation to absorb.

We raise the catch-all's share by removing classes from the vocabulary and
labelling their pixels catch-all, which leaves the catch-all its own single weak
prompt. This is the situation a dataset like LoveDA presents. \best{Each dose arm
is paired with an arity-matched control} that removes the same prompts but labels
those pixels a real class, so the class count changes identically and only the
share differs. Without that control the experiment measures ``fewer classes is an
easier problem''.

\paragraph{Detectability must be scored direction-agnostically.} AUC is symmetric,
so an AUC of $0.208$ is a detector of strength $0.792$ with its sign reversed, not
an absent signal. We report $\mathrm{det} = \max(\mathrm{AUC}, 1-\mathrm{AUC})$
throughout. This is not a technicality: under a variant that merges class
\emph{channels} into the catch-all rather than removing them, the top score does
not degrade at all --- it \emph{inverts}, because a catch-all assembled from
strong prompts is confidently correct about the pixels it absorbed. Scoring raw
AUC would have reported that as a destroyed signal.

\begin{table}[t]\centering\small
\caption{Raising the catch-all's share from \ivShareA{} to \ivShareD{} of ground
truth, against an arity-matched control that applies the same vocabulary change
without raising the share. Detectability is
$\max(\mathrm{AUC}, 1-\mathrm{AUC})$.}
\label{tab:intervention}
\begin{tabular}{lrrrr}
\toprule
signal & published & dose & control & ratio \\
\midrule
\texttt{conf}  & \ivA{}    & \best{\ivB{}}    & \ivD{}    & \ivRatio{} \\
\texttt{conf2} & \ivAtwo{} & \best{\ivBtwo{}} & \ivDtwo{} & \ivRatioTwo{} \\
\bottomrule
\end{tabular}
\end{table}

Both signals degrade with the share, and neither degradation is explained by the
class count (Table~\ref{tab:intervention}). The intervention moves OpenEarthMap
from its own \ivA{} into the $0.58$--$0.62$ band where LoveDA sits, reproducing
the observational regime rather than merely correlating with it. Against the
weakest of the three controls rather than the matched one, the runner-up effect
is still \ivRatioWorst{}.

\paragraph{The label space is the lever, not the prompt list.} A second
intervention separates the two things the dose arms change together. Removing the
catch-all from LoveDA's \emph{vocabulary} while leaving its \emph{label space}
untouched --- \ivSatShare{} of ground truth is still catch-all, those pixels
merely become unnameable --- moves detectability from \ivRevBase{} to \ivRev{},
i.e.\ not at all. Across both datasets, \best{every arm that changes the
vocabulary without changing the catch-all's share of ground truth moves at most
\ivVocabMax{}, while every arm that raises that share moves \ivB{}--\ivBtwo{}}.
The practical consequence is that a catch-all-heavy benchmark cannot be repaired
by editing the prompt list.

\paragraph{The effect saturates, which bounds the claim.} It is concentrated at
the low-share end: above roughly \ivSatShare{} of the scene, four points across
both datasets sit within \ivSatLo{}--\ivSatHi{}, and further increases do nothing.
LoveDA \emph{begins} inside that band, so its own dose arm is underpowered rather
than null --- its dose and its control move by the same magnitude in opposite
directions. Two consequences follow, and we state both. The mechanism is
established where a catch-all grows from rare to roughly a third of the scene, and
not beyond. And \best{share therefore cannot explain the urban--rural gap} in the
stratified evidence, since both strata already lie in the saturated regime; that
stratification eliminates confusability, but the gradient within LoveDA remains
unexplained.

Predictions were registered in the repository before each run and are scored
against the outcome, including four that failed. The effect is
\emph{not} monotone in the interior of the dose range --- the smallest dose is the
minimum --- so we quote the endpoints. Point predictions from a line fitted to the
observational data missed at two of three doses, so the direction transfers and
the rate does not.

% =====================================================================
% =====================================================================
\section{What else we built}
\label{sec:built}

Our original proposal was to recover the residual with a corpus-level semantic
co-occurrence prior over region atoms. The prior is real --- class adjacency
carries \pmiSignal{}~bits of mean absolute pointwise mutual information against a
structure-preserving permutation floor of \pmiFloor{}, a factor of \pmiRatio{} ---
and mining it from the model's own predictions reproduces the ground-truth matrix
at Spearman $\rho=\rhoMined{}$. \best{It still does not earn its place}: against
copying the largest neighbouring region it adds \priorGain{} points, and a
\emph{perfect} matrix adds \priorOracle{}. Adjacent classes are also the confused
ones, so the prior reinforces the errors it was meant to fix. Building it did
produce one result that generalises beyond this paper: pointwise mutual information
over class adjacency must use \emph{boundary} marginals, not the natural choice of
\emph{area} marginals, and correcting that inverted a published finding of our own.
The atom choice matters far more than the prior --- superpixels~\cite{slic} reach
an oracle ceiling of \ceilSLIC{} against \ceilCC{} for connected components.
Details, and the refuted ``road is a hub'' finding, are in the supplementary
material.

\subsection{The method: per-class thresholds}
\label{sec:method}

A single threshold is being asked to serve classes with very different operating
points. On LoveDA, \texttt{water} has precision 89.5\% against
recall 54.7\%: it is right nine times in ten when it fires and finds
half of what is there, so it can afford to fire far more readily.
\texttt{agricultural}, at 66.9\% against 62.0\%, cannot.

We therefore fit one threshold per class by coordinate ascent on a calibration
subset of labelled tiles, and evaluate on disjoint tiles (Fig.~\ref{fig:method}). \best{No model weights
are trained.} The baseline already tunes its single $\tau$ per dataset using labels, $0.5$ for
LoveDA and $0.1$ for OpenEarthMap, so this is the same protocol with $N-1$ free
parameters instead of one. The comparison is parity rather than a leak. The published-$\tau$ baseline is recomputed on the same
held-out tiles.

\paragraph{The objective excludes the catch-all.} We fit to maximise mIoU over
the real classes only. This matters: optimising \emph{full} mIoU allows the search
to buy the metric by repairing an over-predicted background at land cover's
expense, which on OpenEarthMap trades \texttt{road} and \texttt{building} away for
a large \bg{} gain. Full mIoU is still what we report.

\begin{figure*}[t]\centering
\includegraphics[width=\textwidth]{fig7_method.pdf}
\caption{The method. \textbf{(a)}~The fitted thresholds span \eeSpread{} against
a single $0.5$, and move in opposite directions: classes with a large
precision--recall gap fire more readily, classes without one fire less.
\textbf{(b)}~The gain is land cover; the catch-all is untouched. Two protocols
are shown --- five-fold cross-validation and the single end-to-end deployment fit
--- because they agree on the shape and disagree on \bg{}. \textbf{(c)}~The
calibration cost: \calibTiles{} labelled tiles is where every random draw turns
positive.}
\label{fig:method}
\end{figure*}

% =====================================================================
\section{Results}
\label{sec:results}

\begin{table}[t]\centering\small
\caption{The method, five-fold cross-validation. Every LoveDA fold is positive.
On OpenEarthMap land cover improves substantially while full mIoU stays flat;
see the discussion below.}
\label{tab:method}
\begin{tabular}{lrr}
\toprule
 & LoveDA & OpenEarthMap \\
\midrule
$\Delta$ full mIoU, five-fold    & \best{\methodGain{} $\pm$ \methodSD{}} & $+0.16 \pm 0.93$ \\
worst fold                      & \methodWorst{} & --- \\
\midrule
\multicolumn{3}{l}{\emph{both metrics, one partition, identical held-out pixels}}\\
$\Delta$ full mIoU              & \mrLoveFull{} & \mrOemFull{} \\
$\Delta$ excl.\ catch-all       & \best{\mrLoveReal{}} & \best{\mrOemReal{}} \\
catch-all $\Delta$ IoU          & \mrLoveBg{} & \mrOemBg{} \\
\midrule
\texttt{water} alone            & \methodWater{} (at $\tau=\methodWaterTau{}$) & --- \\
oracle bound, full mIoU         & \oracleBound{} & --- \\
share of the bound captured     & \oracleShare{} & --- \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{LoveDA.} Five-fold cross-validation (Table~\ref{tab:method}) gives
\methodGain{}~$\pm$~\methodSD{}~mIoU with every fold positive and a worst fold of
\methodWorst{}. The gain is \best{entirely land cover}: excluding the catch-all
raises the gain to \methodLandMean{}~mIoU while the catch-all itself moves
\methodBg{}~IoU (\methodLand{} summed across the six real classes). That makes
the result structurally immune to the artefact described below, and it captures
\oracleShare{} of an oracle bound of \oracleBound{} --- little is left on the
table. It is driven by \texttt{water}, which gains \methodWater{}~IoU alone at a
fitted threshold of \methodWaterTau{} against a global $0.5$.

\paragraph{OpenEarthMap is positive too, once the catch-all is separated.} Read
as full mIoU the method looks flat there, \mrOemFull{}. Read as
catch-all-excluded mIoU it gains \best{\mrOemReal{}} --- more than LoveDA does.
The headline decomposes exactly: \mrOemFull{} is \mrOemLand{} contributed by the
eight real classes and \mrOemBgC{} contributed by \bg{} alone, whose IoU falls
\mrOemBg{}. \best{The catch-all cancels a real gain, so here the headline
\emph{understates} the method} --- the mirror of Fig.~\ref{fig:oemperclass},
where it overstated it. The per-class movements are ordinary segmentation gains:
\texttt{building} $+4.34$, \texttt{road} $+2.94$, \texttt{pavement} $+2.71$,
\texttt{cropland} $+2.50$. LoveDA is the clean case, where both metrics agree
(\mrLoveFull{} and \mrLoveReal{}) because the catch-all barely moves at
\mrLoveBg{}.

\paragraph{Reporting recommendation.} We therefore report both, and suggest the
field does. That a single averaged score can hide the behaviour it is meant to
summarise is not a new worry~\cite{evalmeasure}; what is new here is a concrete,
measured instance of it in open-vocabulary segmentation, moving the headline in
both directions. mIoU is an \emph{unweighted} mean over $N$ classes, so \best{a
catch-all owns exactly $1/N$ of the metric however meaningful it is} ---
\mrLoveLever{} on LoveDA for a class its annotation guide defines as ``everything
else'', \mrOemLever{} on OpenEarthMap for a class covering $0.84\%$ of the pixels.
A ten-point move in that one class is worth more than most methods claim. The
distortion is present before any method is applied: OpenEarthMap's \bg{} sits at
\mrOemBgIoU{}~IoU against a real-class mean of $47.54$, so its published headline
is depressed by \mrOemDistort{} points by a single pathologically calibrated
class. We have now measured this artefact moving the headline in both directions
--- inflating it in Fig.~\ref{fig:oemperclass}, deflating it here --- which is
why we treat it as a property of the metric rather than of either result.

\paragraph{End-to-end verification.} The results above are computed from the
cached histogram. To confirm that this is a measurement instrument rather than
merely arithmetic, we deployed a single fit --- \calibTiles{} calibration tiles ---
in the actual pipeline. The prediction was \eeBase{}~$\to$~\eeFit{}~mIoU on
\eeTiles{} held-out tiles; the pipeline produced \eeBase{}~$\to$~\eeFit{}, with
every per-class delta agreeing to within $0.04$. Two honest notes. In this single
fit \bg{} gains $+0.85$~IoU, 10\% of the total rather than the
110\% of the artefact below, and incidental --- the objective
excludes it. And \texttt{road} \emph{loses} $0.53$: a per-class rule can hurt a
per-class result, which is why we report the full table rather than the mean.

\paragraph{The gain is not uniform, and the thresholds are domain-specific.}
LoveDA's urban and rural domains differ by a factor of two in discard rate. Fitting
and evaluating within each, the gain is \DOMr{}~$\pm$~\DOMrsd{}~mIoU on rural and
\DOMu{}~$\pm$~\DOMusd{} on urban, which is not distinguishable from zero --- though
land cover improves in both, and urban's catch-all pays for it exactly. The two
domains also want genuinely different thresholds (mean absolute difference
\DOMtaudiff{}, rising to \DOMtaumax{} on \texttt{road}), and calibrating on the
wrong one is \best{worse than not calibrating at all}: \DOMxr{} and \DOMxu{}~mIoU,
both below the published $\tau$. Decomposing the gap, \texttt{water} and
\texttt{forest} account for \cpTop{} of it, and across the twelve (domain, class)
cells the statistic that ranks which classes move is the precision--recall
asymmetry ($\rho=\cpGapRho{}$, $p=\cpGapP{}$) rather than precision itself
($\rho=\cpPrecRho{}$). It does not explain \texttt{water}, the largest single
contributor, whose share and asymmetry are near-identical across the domains while
its gain differs eightfold; about half the gap therefore still lacks a mechanism.
Full tables are in the supplementary material.

\begin{table}[t]\centering\small
\caption{Every intervention we measured. Each is a trivial alternative a reviewer
would otherwise propose, and each is reported with an oracle bound where one
exists.}
\label{tab:interventions}
\begin{tabular}{lrr}
\toprule
intervention & LoveDA & OpenEarthMap \\
\midrule
lower $\tau$ to $0.1$               & \tauCost{} & --- \\
remove presence gating              & \presCost{} & --- \\
co-occurrence prior over neighbours & \priorGain{} & --- \\
honest recovery                     & \recoverLove{} & \recoverOEM{}\,$^\dagger$ \\
per-class $\tau$, label-free        & $-0.17$ & $+4.77$\,$^\dagger$ \\
\best{per-class $\tau$, calibrated} & \best{\methodGain{}} & $+0.16$ \\
\bottomrule
\end{tabular}\\[3pt]
\footnotesize $^\dagger$ catch-all artefact --- see Fig.~\ref{fig:oemperclass}.
\end{table}
\begin{figure}[t]\centering
\includegraphics[width=\columnwidth]{fig3_oem_per_class.pdf}
\caption{Per-class decomposition of OpenEarthMap's \recoverOEM{}~mIoU from
recovery. \bg{} gains \oemBgGain{}~IoU while the eight real classes lose
\oemRealLoss{} and \texttt{building}, the baseline's strongest class, loses
\oemBuildLoss{}. A background-assigned real-class pixel is already wrong, so
relabelling it either fixes it or leaves it wrong in a different class --- while
\bg{} sheds false positives either way. Averaging over nine classes converts a
worse segmentation into a better number.}
\label{fig:oemperclass}
\end{figure}
\begin{figure}[t]\centering
\includegraphics[width=\columnwidth]{fig4_detection_auc.pdf}
\caption{Nine detection signals on both datasets against an empirical floor of
\aucFloor{}. On LoveDA every signal lands between $0.434$ and \aucLove{}; on
OpenEarthMap the same signals separate the residual well. The floor is empirical
rather than $0.5$ because region-level features carry a size confound, which we
control for by computing AUC within size-matched bins.}
\label{fig:auc}
\end{figure}

\subsection{Does it transfer to another pipeline?}
\label{sec:transfer}

A correction fitted on one pipeline invites the obvious objection that it exploits
a quirk of that pipeline. We test it on the nearest published competitor, which is
CLIP-based rather than SAM~3-based and thresholds its per-class scores at its own
tuned values (\cnTau{} on LoveDA). Nothing about the method changes: we cache its
per-pixel scores and run the identical fit.

\begin{table}[t]\centering\small
\caption{The same correction, two pipelines and two datasets. $\Delta$ full mIoU,
five-fold, calibration and evaluation tiles disjoint, each baseline recomputed on
its own held-out tiles at its own published threshold. \best{The axis that
separates success from failure is the dataset, not the backbone.}}
\label{tab:transfer}
\begin{tabular}{lrr}
\toprule
 & LoveDA & OpenEarthMap \\
\midrule
SAM~3 pipeline  & \best{\methodGain{} $\pm$ \methodSD{}} & \mrOemFull{} \\
CLIP pipeline   & \best{\cnCV{} $\pm$ \cnCVsd{}} & \cnOemCV{} $\pm$ \cnOemCVsd{} \\
\midrule
\multicolumn{3}{l}{\emph{excluding the catch-all class}}\\
SAM~3 pipeline  & \mrLoveReal{} & \mrOemReal{} \\
CLIP pipeline   & \cnReal{} & \cnOemReal{} \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{On LoveDA it transfers, and it is larger.} The CLIP pipeline gains
\cnCV{}~$\pm$~\cnCVsd{}~mIoU against \methodGain{}~$\pm$~\methodSD{} for our own,
with every fold positive, a worst fold of \cnWorst{}, and \emph{every} class
improving (\cnFull{} full mIoU on one partition) --- which our SAM~3 result does
not manage. It also needs an eighth of
the calibration data, \cnCalib{} tiles against \calibTiles{}. In absolute terms
their pipeline moves \cnBase{}~$\to$~\cnFit{}~mIoU, so \best{our contribution
composes with the competitor rather than displacing it}.

\paragraph{On OpenEarthMap it transfers to neither, and the failure is shared.}
The CLIP pipeline gives \cnOemCV{}~$\pm$~\cnOemCVsd{} across folds, one of five
positive, and \cnOemFull{} on a single partition.
But land cover still improves (\cnOemReal{} excluding the catch-all); what cancels
it is \bg{}, whose IoU falls \cnOemBg{}. \best{Our own pipeline does the same thing
on the same dataset}, and to a striking degree: the fit drives that class to
\mrOemBgAfter{}~IoU for SAM~3 and \cnOemBgAfter{} for CLIP, from a starting value
near $17$ in both cases. Two unrelated models, the same sacrifice, the same
endpoint.

\best{So the correction is not backbone-specific; it is dataset-specific, and the
axis is how well the catch-all is calibrated.} Where that class is competently
predicted, the fit improves land cover \emph{and} the headline, on both
architectures. Where it is pathological --- OpenEarthMap's \bg{} sits at
\mrOemBgIoU{}~IoU and owns $1/N$ of a nine-class mean --- an optimiser told to
maximise land cover trades it away, and no headline gain is possible on either
pipeline. This is the same conclusion Section~\ref{sec:results} reaches from our
own results, arriving independently through a competitor's code.

\paragraph{Two caveats.} The deltas are measured on our own reproduction of their
pipeline, which reaches \cnRepro{}~mIoU on LoveDA against their published \cnPub{}.
A delta is robust to a constant offset, but we report the discrepancy rather than
only the gain --- and the OpenEarthMap reproduction is considerably further off, so
we treat that row as supporting rather than independent evidence. On LoveDA their
catch-all gains \cnBg{}~IoU, about a third of the full-mIoU movement; far from the
pathological case of Fig.~\ref{fig:oemperclass}, and the catch-all-excluded column
is independently positive, but we quote both as everywhere else.

% =====================================================================
\subsection{The label-free bound, and why it holds}
\label{sec:bound}

Sweeping a single global $\tau$ finds \globalOracle{}~mIoU on LoveDA, which is why
threshold tuning is usually considered closed. Sweeping a threshold \emph{per
class} is worth \oracleBound{} --- more than thirty times as much --- with the
real classes gaining $+8.63$~IoU. The chosen thresholds span \eeSpread{}: one
global value is wrong for different classes in opposite directions.

We tested \nAttempts{} label-free rules against that bound: three threshold rules
(per-class Otsu~\cite{otsu}, a confidence percentile, and presence-scaled
thresholds) and
eight per-class statistics converted into thresholds by a single fitted knob ---
the same one-parameter budget the baseline spends on its own $\tau$. \best{None
reaches it.} The best scores \proxyBest{}~mIoU against a random-proxy control
whose 95th percentile is \proxyCtrl{}; on LoveDA every one of the three threshold
rules scores \emph{below} the published $\tau$. The control is not optional: one
fitted knob over six classes can extract a gain from a meaningless vector.

\paragraph{The reason is not the one we first proposed.} We had supposed the
oracle exploits per-class \emph{precision}, which is label-derived by definition.
That is false. Precision is predictable without labels: mean confidence ranks the
six classes by precision at $\rho=\proxyConfRho{}$ ($p=\proxyConfP{}$), and agreement between SAM~3's two heads at $\rho=\proxyHeadRho{}$
($p=\proxyHeadP{}$), a statistic available only because we instrument the heads
separately where the baseline fuses them with a maximum. Both $p$-values are exact, by
enumerating all $720$ relabelings, because at six classes $|\rho|=0.6$ arises by
chance roughly a fifth of the time. Both move mIoU by less than $0.1$.

\paragraph{The restated bound.} Precision does not determine the threshold. The
relation is not even monotone: \texttt{water} at 88.5\% precision
takes $\tau=0.175$, \texttt{road} at 68.5\% takes $0.675$, and
\texttt{barren} at 55.6\% takes $0.375$
($\rho=\proxyTauRho{}$, $p=\proxyTauP{}$). \best{The optimal per-class threshold
is the solution to a coupled multi-class objective}: raising one class's threshold
moves its pixels into \bg{}, changing \bg{}'s IoU and therefore every other
class's optimum. No per-class scalar can express that, measurable or not. This
explains why an $N$-parameter fit works and every one-parameter rule fails, and
it generalises to any training-free pipeline that thresholds a per-class score.

\paragraph{The same bound governs whether calibration is worth commissioning.}
Since the gain is uneven across domains, a practitioner
needs to know in advance whether their data is the kind where it pays --- and the
catch-all fraction is computable from predictions alone, with no ground truth
(it ranks tiles almost identically to the labelled discard rate,
$\rho=\crProxyRho{}$ on LoveDA and $\crProxyRhoOEM{}$ on OpenEarthMap). It does
not work. Stratifying LoveDA by it gives a \emph{U-shaped} profile rather than a
monotone one: the largest and by far the most reliable gain, \crLow{}, falls in
the stratum with the \emph{least} residual, while the most-affected stratum has
the biggest mean and is useless as guidance at \crHigh{}, with a negative worst
fold. Across strata $\rho=\crRho{}$, and on OpenEarthMap $\crRhoOEM{}$ --- the
opposite sign, with random strata of the same sizes moving nearly as far.

This is not a separate negative. The gain lives in classes with a large
precision--recall asymmetry, and which classes those are \emph{is} per-class
precision: the quantity that is measurable without labels and still does not
determine the threshold. \best{So one bound covers both halves --- the labels
cannot be avoided in choosing the thresholds, and they cannot be avoided in
deciding whether to choose them.} The calibration set buys the answer and the
question at the same time, and its cost is irreducible.

% =====================================================================
\section{Limitations}
\label{sec:limitations}

\paragraph{Two datasets.} The mechanism is established by intervention
(Section~\ref{sec:intervention}) on one of them, and the stratified evidence
supporting it comes from the other. A third dataset whose catch-all is common but
visually distinct would strengthen both, and we did not have one: the obvious
candidate, iSAID, has a catch-all that is 97.11\% of ground truth \emph{and}
maximally confusable, so it would confirm the ordering and discriminate nothing.
The doses in Section~\ref{sec:intervention} are chosen greedily from the smallest
classes upward rather than at random, and the arity-matched control is not
perfectly inert, since dropping a prompt disturbs the scores wherever that class's
pixels lie. We therefore report the dose-to-control \emph{ratio} rather than the
difference alone.

\paragraph{Calibration must match the evaluation distribution, and the gain is
not uniform.} Fitting on LoveDA's \emph{train} split and evaluating on val gives
\trainToVal{}~mIoU, because those splits differ sharply in discard rate at
identical background share. The supplementary material bounds this at a
controlled calibration budget: thresholds fitted on one LoveDA domain and applied to the
other give \DOMxr{} and \DOMxu{}~mIoU, \emph{below} the published $\tau$, so the
wrong domain's calibration is an active harm rather than a diminished benefit.
The method requires calibration data from the deployment distribution, and
\calibTiles{} tiles of it; below fifty tiles it actively hurts. Within LoveDA the
gain is also concentrated: \DOMr{} on rural against \DOMu{} on urban, so our
headline \methodGain{} is a benchmark-level figure and not a claim about every
stratum of it. We regard this as the sharpest limitation of the work: the method
is a calibration correction, and calibration is exactly the thing that does not
transport.

\paragraph{Oracle bounds are bounds under our labeller.} Every ``oracle'' figure
we report gives one component perfect information while holding the rest of our
implementation fixed. They bound what our design could achieve, not what is
achievable in principle.

\paragraph{Scope.} OpenEarthMap is evaluated at \oemTiles{} of 500 tiles.
Recovery is evaluated at one threshold per dataset --- each dataset's own tuned
value --- though Section~\ref{sec:bound} bounds what any other threshold could
have given. Deep features remain untested for detection; we judged this
unnecessary because detection already succeeds on OpenEarthMap (AUC \aucOEM{})
and recovery still fails there, so better detection buys nothing.

% =====================================================================
\section{Conclusion}
\label{sec:conclusion}

The residual that presence-gated segmentation pipelines discard is a property of
the annotation scheme as much as of the model, and whether it can be detected
follows the same axis. Neither regime makes recovery worthwhile: where the
residual is hard to find it cannot be found, and where it is easy to find the
resulting labels are not accurate enough to beat leaving it alone. What does work is cheaper and less interesting to describe: fitting the threshold
per class rather than globally. It works because a single threshold is being asked
to serve classes with incompatible operating points. It is positive on both datasets
once the catch-all is separated out, and on one of them that separation is the
difference between a result and a null. We give the bound on the idea, show it
cannot be reached without labels, and identify why: the quantity that sets the
right threshold is not a per-class property at all.

The two halves meet in the metric. A catch-all class distorts what is measured as
well as what is predicted, and because mIoU is an unweighted mean it does so with
leverage $1/N$ regardless of how meaningful that class is. We would rather the
field reported the real-class mean beside the headline than continue to discover
this one dataset at a time.

We would emphasise one methodological point. Four of the results in this paper
are refutations of our own earlier claims, each caught by a test written on
purpose --- a null model that inverted a published finding of ours, a supervision
leak that produced a plausible and quotable number, a detector that scored $0.966$
on random noise, and an intervention whose apparent success was a detector
changing sign rather than losing information. Reporting a headline mIoU gain without decomposing it
per class is, on this evidence, the single easiest way to publish something that
is not true.

\bibliographystyle{plain}
\bibliography{refs}
\end{document}
